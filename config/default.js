const config = {
  // 启动端口
  server: {
    PORT: 3000,
  },

  // 数据库配置
  database: {
    DATABASE: 'test',
    USERNAME: 'root',
    PASSWORD: 'root',
    PORT: 13306,
    HOST: 'localhost',
  },
}

module.exports = config
